ADD_LIBRARY(service MODULE initng_service.c)
TARGET_LINK_LIBRARIES(service initng)
INSTALL_TARGETS(${LIB_INSTALL_DIR}/initng service)

SET_TARGET_PROPERTIES(service PROPERTIES LINK_FLAGS ${MOD_LDFLAGS})
