ADD_LIBRARY(unneeded MODULE initng_unneeded.c)
TARGET_LINK_LIBRARIES(unneeded initng)
INSTALL_TARGETS(${LIB_INSTALL_DIR}/initng unneeded)

SET_TARGET_PROPERTIES(unneeded PROPERTIES LINK_FLAGS ${MOD_LDFLAGS})
