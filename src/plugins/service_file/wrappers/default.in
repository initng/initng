#!/bin/sh

eval "
${CMD}()
{
	echo \"${SERVICE}: command ${CMD} not found.\"
	return 1
}
"

ireg()
{
	[ -n "${2}" -a "${SERVICE}" != "${2}" ] && return 1
	iregister "${1}" || exit ${?}
	_atexit="idone"
}

export PATH="@LIB_INSTALL_DIR@/ibin:${PATH}"
. "${SFILE}"
${CMD}
${_atexit}
