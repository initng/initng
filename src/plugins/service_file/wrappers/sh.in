#!/bin/sh
export PATH="@LIB_INSTALL_DIR@/ibin:$PATH"

if [ "$1" = "setup" ]; then
	iregister service || exit
	iset need = system/bootmisc
	iset syncron = compat
	iexec start
	iexec stop
	idone
	exit 0
fi

. "$SFILE"
