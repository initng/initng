ADD_LIBRARY(fmon MODULE initng_fmon.c)
TARGET_LINK_LIBRARIES(fmon initng)
INSTALL_TARGETS(${LIB_INSTALL_DIR}/initng fmon)

SET_TARGET_PROPERTIES(fmon PROPERTIES LINK_FLAGS ${MOD_LDFLAGS} COMPILE_FLAGS ${MOD_CFLAGS})
