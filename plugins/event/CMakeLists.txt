INCLUDE_DIRECTORIES( ${CMAKE_SOURCE_DIR}/src/include ${CMAKE_BINARY_DIR} )

ADD_LIBRARY(event MODULE initng_event.c)
TARGET_LINK_LIBRARIES(event initng)
INSTALL_TARGETS(${LIB_INSTALL_DIR}/initng event)

