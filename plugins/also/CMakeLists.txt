ADD_LIBRARY(also MODULE initng_also.c)
TARGET_LINK_LIBRARIES(also initng)
INSTALL_TARGETS(${LIB_INSTALL_DIR}/initng also)

SET_TARGET_PROPERTIES(also PROPERTIES LINK_FLAGS ${MOD_LDFLAGS} COMPILE_FLAGS ${MOD_CFLAGS})
