ADD_LIBRARY(stdout MODULE initng_stdout.c)
TARGET_LINK_LIBRARIES(stdout initng)
INSTALL_TARGETS(${LIB_INSTALL_DIR}/initng stdout)

SET_TARGET_PROPERTIES(stdout PROPERTIES LINK_FLAGS ${MOD_LDFLAGS} COMPILE_FLAGS ${MOD_CFLAGS})
