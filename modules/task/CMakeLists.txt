ADD_LIBRARY(task MODULE initng_task.c)
TARGET_LINK_LIBRARIES(task initng)
INSTALL_TARGETS(${LIB_INSTALL_DIR}/initng task)
SET_TARGET_PROPERTIES(task PROPERTIES LINK_FLAGS ${MOD_LDFLAGS} COMPILE_FLAGS ${MOD_CFLAGS})
