#define VERSION			"${VERSION}"
#define VERSION_NAME		"${VERSION_NAME}"
#define CHECK_RO
#define FORCE_NOCOLOR
#define FORCE_POSIX_IFILES
#define RUNLEVEL_DEFAULT	"${RUNLEVEL_DEFAULT}"
#define RUNLEVEL_FAKE		"${RUNLEVEL_FAKE}"
